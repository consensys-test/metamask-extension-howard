import { strict as assert } from 'assert';

describe('E2E matrix completion fixture', function () {
  it('fails in the Chrome Webpack matrix', function () {
    assert.fail('Intentional merge-queue matrix failure');
  });
});
